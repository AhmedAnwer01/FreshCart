import { BestProductsPipe } from './best-products.pipe';

describe('BestProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new BestProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
